# protobuf-wire-protocol

Replace the hand-rolled JSON-over-WebSocket protocol with Protobuf-over-WebSocket. Schema becomes one .proto file, stubs are generated for Python and TypeScript, structured logging makes the connection debuggable, and a debug CLI client lets you inspect/replay frames with standard tooling.
